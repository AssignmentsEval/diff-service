server:
  port: 7080
  servlet:
    context-path: /api/diff-service
  urlPatterns: /v1/*
management:
  endpoints:
    web:
      exposure:
        include: info,health
spring:
  data:
    mongodb:
      database: diff
  output:
    ansi:
      enabled: always
